# Server
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# MongoDB
# Local example:
# MONGODB_URI=mongodb://127.0.0.1:27017/parhly
# Atlas example (replace user/pass, cluster host, and db name). URL-encode special characters in password.
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/parhly?retryWrites=true&w=majority&appName=parhly
MONGODB_URI=mongodb://127.0.0.1:27017/parhly

# JWT
JWT_SECRET=web_technologies_wt_token
JWT_EXPIRES_IN=2h
